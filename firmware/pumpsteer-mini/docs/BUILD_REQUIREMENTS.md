# Build requirements

PumpSteer Mini is developed for ESP-IDF and C++17 host tests.

Required tools:
- ESP-IDF with ESP32-S3 support
- CMake 3.16 or newer for host tests
- a C++17 compiler

The exact ESP-IDF release will be pinned before the first public firmware release after the hardware build has been verified end-to-end.
