# Development notes

This directory is intended to become the root of the standalone `JohanAlvedal/PumpSteer-Mini` repository.

Before migration, keep all new Mini-specific source, documentation, tests and CI files inside this directory so the move remains a simple directory-to-root copy.
