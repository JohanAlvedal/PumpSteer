name: HACS Validation

on:
  push:
    paths:
      - 'custom_components/**'
  pull_request:
    paths:
      - 'custom_components/**'

jobs:
  hacs:
    name: HACS Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: hacs/action@main
